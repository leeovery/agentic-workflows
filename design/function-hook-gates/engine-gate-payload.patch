diff --git a/.claude/skills/workflow-engine/scripts/domain/projections/surfaces.cjs b/.claude/skills/workflow-engine/scripts/domain/projections/surfaces.cjs
index c07b9b9..5d2e3e7 100644
--- a/.claude/skills/workflow-engine/scripts/domain/projections/surfaces.cjs
+++ b/.claude/skills/workflow-engine/scripts/domain/projections/surfaces.cjs
@@ -162,13 +162,51 @@ function alignOptions(lines, { width = displayWidth(), skip = 0 } = {}) {
   return out;
 }
 
+// A gate's options, as data rather than as the markdown that displays them.
+// Every option row records itself here as it is composed; a render reads the
+// collection and emits it beside the MENU so a surface can draw the gate
+// without parsing the presentation back out. Synchronous per render, reset at
+// the top of each one (`openGate`), so a row can only land in its own gate.
+let gateRows = null;
+
+/** Begin collecting a gate's rows. @returns {void} */
+function openGate() {
+  gateRows = { options: [], typed: [] };
+}
+
+/** Take what was collected, and stop collecting. @returns {{options: object[], typed: object[]}|null} */
+function takeGate() {
+  const rows = gateRows;
+  gateRows = null;
+  return rows;
+}
+
+/**
+ * The GATE section: the same options the MENU shows, as JSON, for a surface
+ * that draws them. Never displayed, never parsed by the model — it exists so
+ * a renderer does not have to read the markdown the MENU is made of.
+ * @param {{options: object[], typed: object[]}} rows @param {string} name @returns {string}
+ */
+function gateSection(rows, name) {
+  const payload = JSON.stringify({
+    gate: name,
+    question: rows.question ?? '',
+    options: rows.options,
+    typed: rows.typed,
+  });
+  return `=== GATE (json for a gate surface — never display, never parse the MENU below) ===\n${payload}\n`;
+}
+
 /**
  * One `=== NAME (instruction) ===` demarcated section.
  * @param {string} name @param {string} instruction @param {string} body
  * @returns {string}
  */
 function section(name, instruction, body) {
-  return `=== ${name} (${instruction}) ===\n${body.replace(/\n+$/, '')}\n`;
+  const block = `=== ${name} (${instruction}) ===\n${body.replace(/\n+$/, '')}\n`;
+  if (!name.startsWith('MENU')) return block;
+  const rows = takeGate();
+  return rows === null ? block : gateSection(rows, name.replace(/^MENU:?\s*/, '') || 'menu') + block;
 }
 
 // The instructions for a DISPLAY that is the whole response: emitting it
@@ -206,6 +244,7 @@ const AUTO_GATE_INSTRUCTION = 'emit verbatim as a code block — the user set th
 function menuFrame(lines, { glyphLabel = true, width, skip = 0 } = {}) {
   const body = alignOptions(lines, { width, skip });
   if (glyphLabel && body.length > 1 && body[1] === '' && isGlyphable(body[0])) {
+    if (gateRows !== null && !gateRows.question) gateRows.question = body[0];
     body[0] = `**\`${MENU_GLYPH} ${body[0]}\`**`;
   }
   consentAsks(body);
@@ -262,6 +301,7 @@ function isGlyphable(label) {
  * @returns {string}
  */
 function menu(label, options, { prompt, question } = {}) {
+  if (gateRows !== null && gateRows.question === undefined) gateRows.question = question ?? label ?? '';
   const lines = label ? [label, ''] : [];
   // A yes/no gate whose label is a statement carries its ask separately: the
   // statement stays context, the short question takes the decision glyph.
@@ -285,6 +325,7 @@ function menu(label, options, { prompt, question } = {}) {
  * @returns {string}
  */
 function cmdOption(key, word, label) {
+  if (gateRows !== null) gateRows.options.push({ key: String(key), word: word ?? null, label });
   return `**\`${word ? `${key}/${word}` : key}\`** → ${label}`;
 }
 
@@ -297,6 +338,8 @@ function cmdOption(key, word, label) {
  * @returns {string}
  */
 function bareOption(key, word) {
+  // A labelless row — the word is the whole of it, so it is its own label.
+  if (gateRows !== null) gateRows.options.push({ key: String(key), word: word ?? null, label: word ?? String(key) });
   return `**\`${key}/${word}\`**`;
 }
 
@@ -308,6 +351,7 @@ function bareOption(key, word) {
  * @returns {string}
  */
 function promptOption(label, description) {
+  if (gateRows !== null) gateRows.typed.push({ label, description });
   return `**${label}** → ${description}`;
 }
 
@@ -318,6 +362,9 @@ function promptOption(label, description) {
  * @returns {string}
  */
 function rangeOption(first, last, label) {
+  // A span of numbers, and several may be given at once: nothing a surface can
+  // offer as one press, so it states it as a row the person types.
+  if (gateRows !== null) gateRows.typed.push({ label: `${first}–${last}`, description: label });
   return `**\`${first}–${last}\`** → ${label}`;
 }
 
@@ -391,5 +438,8 @@ function treeList(items, { indent = '     ', width = displayWidth() } = {}) {
   return out.join('\n');
 }
 
-module.exports = { DOTS, MENU_GLYPH, section, CONTINUE_INSTRUCTION, CONTINUE_MARKDOWN_INSTRUCTION, AUTO_GATE_INSTRUCTION, menuFrame, alignOptions, menu, cmdOption, bareOption, promptOption, rangeOption, callout, indentedBody, bulletRow, subDetail, treeList };
+module.exports = {
+  openGate,
+  takeGate,
+  gateSection, DOTS, MENU_GLYPH, section, CONTINUE_INSTRUCTION, CONTINUE_MARKDOWN_INSTRUCTION, AUTO_GATE_INSTRUCTION, menuFrame, alignOptions, menu, cmdOption, bareOption, promptOption, rangeOption, callout, indentedBody, bulletRow, subDetail, treeList };
 
diff --git a/.claude/skills/workflow-engine/scripts/domain/render.cjs b/.claude/skills/workflow-engine/scripts/domain/render.cjs
index 2f375a1..a0ce2c2 100644
--- a/.claude/skills/workflow-engine/scripts/domain/render.cjs
+++ b/.claude/skills/workflow-engine/scripts/domain/render.cjs
@@ -20,6 +20,7 @@ const { TREE_WIDTH, titlecase, WORKLIST_GLYPH, DISCOVERY_GLYPH, discoveryLifecyc
 const { section, CONTINUE_INSTRUCTION, CONTINUE_MARKDOWN_INSTRUCTION, AUTO_GATE_INSTRUCTION, menu, menuFrame, MENU_GLYPH, cmdOption, bareOption, promptOption, callout, indentedBody, bulletRow, subDetail, treeList } = require('./projections/surfaces.cjs');
 const { buildOrderLive } = require('./build-order.cjs');
 const { worklist, escapeMarkdown } = require('./projections/worklist.cjs');
+const { openGate } = require('./projections/surfaces.cjs');
 const { blockedTasksMenu, taskGateSection, fixGateSection, cycleLimitDisplay, specCorrectionsDisplay, cycleGateMenu } = require('./projections/tasks.cjs');
 const { workunitReceipt, topicReceipt, absorbSummary, absorbReceipt, promoteReceipt, pivotContinuationMenu, absorbContinuationMenu, sessionReceipt } = require('./projections/transactions.cjs');
 const { absorbTargetMenu, absorbNameGate, absorbConfirmGate, planTopicsMenu, archivedActions, archivedDeleteGate } = require('./projections/start.cjs');
@@ -5226,6 +5227,9 @@ const SURFACES = {
  * @returns {string}
  */
 function renderSurface(cwd, surface, args) {
+  // Collect this render's gate rows as they are composed; `section` emits them
+  // as a GATE block beside the MENU, and drops them when there is no menu.
+  openGate();
   const handler = SURFACES[surface];
   if (!handler) {
     throw new Error(`render: unknown surface "${surface}" (surfaces: ${Object.keys(SURFACES).join(', ')})`);
diff --git a/.claude/skills/workflow-engine/scripts/gateway.cjs b/.claude/skills/workflow-engine/scripts/gateway.cjs
index 120a262..84f7202 100644
--- a/.claude/skills/workflow-engine/scripts/gateway.cjs
+++ b/.claude/skills/workflow-engine/scripts/gateway.cjs
@@ -24,6 +24,8 @@
  *   `gateway.cjs {work_unit}`).
  */
 
+const { openGate, takeGate, gateSection } = require('./domain/projections/surfaces.cjs');
+
 const SECTION = {
   title:   '=== TITLE (emit verbatim as markdown — the view\'s chrome heading) ===',
   data:    '=== DATA (reason from this — never display or parse the sections below) ===',
@@ -51,7 +53,9 @@ function displayBlock(body) {
 
 /** @param {string} body menu block, pre-rendered */
 function menuBlock(body) {
-  return SECTION.menu + '\n' + String(body).replace(/\n+$/, '') + '\n';
+  const block = SECTION.menu + '\n' + String(body).replace(/\n+$/, '') + '\n';
+  const rows = takeGate();
+  return rows === null ? block : gateSection(rows, 'menu') + block;
 }
 
 // `key: value` lines for flat values; nested objects/arrays render as compact
@@ -71,6 +75,8 @@ function dataLines(obj) {
  */
 function runGateway(handlers, argv = process.argv.slice(2)) {
   const [first, ...rest] = argv;
+  // As in `renderSurface`: a handler's options record themselves while it runs.
+  openGate();
 
   let out;
   if (first === undefined) {
