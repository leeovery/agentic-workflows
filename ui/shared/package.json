{
  "name": "@workflow-ui/shared",
  "private": true,
  "version": "0.0.0",
  "description": "Gate-card schema and event vocabulary as zod schemas — provisional until the end-of-Phase-2 freeze.",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
