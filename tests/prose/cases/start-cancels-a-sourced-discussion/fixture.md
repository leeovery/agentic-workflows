A search-relevance epic past its grouping analysis: both discussions
(behavioural-ranking, synonym-handling) concluded in earlier sittings,
and one specification, "expansion", started over the pair — both
source rows still pending, a stub document on disk.

The user returns via workflow-start to abandon the synonym-handling
topic. Cancelling it cannot be a plain cancel: a live specification is
built from it, so the engine refuses the bare transaction, the epic
menu presents the engine-rendered collapse confirm naming what falls,
and on the user's confirmation one cascade transaction cancels the
discussion and the expansion specification together — both
reactivatable, both recorded.
