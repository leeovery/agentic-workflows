The prose should have taken this path:

1. the entry resolves the topic from its arguments, reads the research
   status, finds it in progress, emits the resuming phase note, checks
   the reconcile flag (absent — silent), and hands off to the processing
   skill without asking the user anything
2. the process reads the status again, finds the file, renders the
   thread register once above resume detection — four threads, one
   being dug — and the user continues; initialisation is skipped
3. the walk passes through file strategy and the guidelines, addresses
   the knowledge base once as a contextual query, and routes into the
   epic research session — the deep-dive and rerouted-concerns
   protocols loaded, nothing run at load
4. the user's first words are the done-signal, so the wrapper routes to
   its in-flight handling — and that handling enters the fold before it
   reads anything else: the scan answers one pending row,
   `deep-dive-001-tail-regressions`, nothing in flight, nothing dead
5. the fold runs as one transaction of judgment: the report is read in
   full; a section headed by the thread's question, the dive's id, and
   the date is written into the research file — the answer first, then
   the material, sources inline; the thread is set `learned` and the row
   closed clean; the Opened list is judged line by line — the tail-group
   size question becomes a thread of origin `deep-dive-001` nested under
   the folded thread, and the measurement becomes a thread the same way,
   what it would settle as its question, also nested under it — and one
   commit lands with the dive's id in its subject
6. the session speaks the return — the brief asked one question, so its
   answer reaches the user in full, at product altitude — and renders the
   register: the folded thread learned with its two opened children
   beneath it
7. the fold asked the user nothing, so the wrapper re-scans: nothing in
   flight, no gate, and the close proceeds — the triage queue reads
   empty, the wait gate answers empty, the document review reconciles
   the file against a conversation that added nothing, and the
   compliance check passes
8. the conclude gate renders with the register above its menu — the
   learned threads, the open ones (the user's label-freshness question
   and both opened children), the parked one with its reason — and the
   user concludes; no dead-end row is passed
9. before the completion records, the open and parked threads are
   written into the file's `## Open Threads` — the label-refresh
   question, the tail-group size question, the tail-share measurement,
   the human-raters panel with its parked note — then `topic complete`,
   the `--kb` commit, the sweep, the closing recap, the discussion
   signpost, and the bridge hand-off, where the walk stops

Claims — the close's fold is the behaviour under test:

- the landed report is folded before the conclusion reads the in-flight
  gate: the section is in the file, the thread is learned, the row is
  incorporated, and none of it waited for a later break
- the answer opens on the product, not the report — what happens to a
  change that lifts most searches and drops the rare ones is on the
  page before any file, function, or table is named; no code identifier
  from the report reaches the user, and the report is never pasted
- the measurement line is carried, not offered: no laboratory offer is
  made on the way out, no experiment is created, and the measurement
  reaches the discussion as an open thread in Open Threads
- the register renders once after the fold and once more above the
  conclude gate — never twice for one state
- no dispatch of any kind, no dive offer for the opened question, no
  review, no in-flight gate (nothing was in flight)

EXPECTED WORLD — the fixture plus: the research file carrying a new
section headed by the folded question, `deep-dive-001`, and the date,
answer first then material, and a closing `## Open Threads` naming the
label-refresh question, the two opened threads, and the parked
human-raters line with its note; the register with `tail-regressions`
learned and two child threads under it of origin `deep-dive-001` and
status `open`; the store row `deep-dive-001-tail-regressions`
`incorporated`; the research item `completed`; no experiment item, no
change to the map or the sibling topics.
