# Fixture — spec-grouping-births-the-build-order

The `search-relevance` epic has two concluded discussions —
`behavioural-ranking` and `synonym-handling`, both decided on their
maps — and a third map topic, `relevance-measurement`, still fresh and
research-routed. The discovery gap analysis is stamped over the two
conclusions. No specification item exists anywhere, no discussion is
in-progress, and `phases.specification` carries no `build_order_stale`
flag.

The discovery map is fully sequenced (orders 1–3 from the harvest) —
those numbers rank exploration, and the grouping analysis must not
inherit them.
