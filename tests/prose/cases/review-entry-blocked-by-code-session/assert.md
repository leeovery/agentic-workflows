The prose should have taken this path:

1. resolves the topic to the work unit
2. the prerequisite gate renders empty — both the plan and the
   implementation are complete, so nothing about this work unit blocks
   entry
3. no review item exists, so the fresh path is taken — nothing is
   reopened and no resume is announced
4. the code-slot check runs after the phase validation, reads the whole
   project rather than this work unit, and comes back carrying the gate:
   both its sections are emitted verbatim — the blocking fact naming who
   holds the slot, where, and how long ago they were active, then the
   menu
5. the walk stops there for the user's answer — the gate is a stop, not a
   notice
6. on `back` the leave-it-alone line is emitted and the walk ends;
   nothing further runs

Further claims:

- the handoff is never constructed: the plan's format is not read and the
  review processing skill is never invoked
- no verifier agents are dispatched
- the holding session's heartbeat is left exactly as it was — the gate
  names how to release it, and reading a gate never releases anything

EXPECTED WORLD — the fixture, unchanged.
