The prose should have taken this path:

1. the entry resolves the topic from its arguments, reads the research
   status, finds it in progress, emits the resuming phase note, checks
   the reconcile flag (absent — silent), and hands off to the processing
   skill without asking the user anything; the epic context arm reads
   the map item's source, finds it map-shaped rather than direct-start,
   and gathers nothing
2. the process reads the status again, finds the file, renders resume
   detection, and the user continues; initialisation is skipped
3. the walk passes through file strategy and the guidelines, addresses
   the knowledge base once as a contextual query, and routes into the
   epic research session
4. the user signals they are done. The session loop's triage check reads
   the queue and finds it empty; no agent is in flight, so the walk
   enters topic completion
5. topic completion re-reads the triage queue — still empty — and runs
   the closing gates in order: the final review dispatches (no review
   row has ever existed here), the stubbed report returns clean, the
   row is acknowledged clean and nothing is raised; document review
   reconciles the session against the file and finds it already says
   what the session said; the compliance check passes silently
6. the session judges both gate questions before rendering: no dead end
   — the finding is exactly what the topic's next phase consumes — so
   the gate renders **without** `--dead-end`; and the research surfaced
   a decision-bearing hypothesis only a controlled run can settle, so
   the session recommends the measure route in one line before the gate.
   The `e/experiment` row is on the menu either way — it always renders
7. the user takes the measuring route. The conclusion runs exactly as a
   normal conclusion — queue checked a final time, the research
   completed and indexed by the engine, one `--kb` commit — the evidence
   trail unbroken; the sweep finds no leavings and the closing recap
   runs
8. the closure signpost is the experiment one — research complete,
   routing into experiments, a designed measured run before the
   discussion reads it — and the walk stops at the invocation of the
   experiment entry with the same topic. It is **not** the bridge: the
   route is straight-through with context hot, no context clear

Further claims:

- the gate rendered without the dead-end flag, and no map item was
  marked handled
- the research item is `completed`, indexed like any concluded research
  — the experiment arm changes the route, never the conclusion's shape
- nothing experiment-side was created: no experiment item, no series
  record — the entry skill the walk stops at owns that
- no discussion was started and no bridge was invoked
- the research document's substance is unchanged — the session added
  nothing, and document review had nothing to reconcile

EXPECTED WORLD — the fixture plus: `synonym-handling`'s research item
`completed`; the stubbed review report on disk in the topic's cache with
its agent row closed; the knowledge store carrying the research chunks;
and no experiment item, no discussion item, no map changes, and the
other two topics untouched.
