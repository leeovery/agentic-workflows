The `search-relevance` epic, three topics on the harvested map. The
research-routed `synonym-handling` topic has been explored to the edge
of conclusion: behaviour-driven expansion is the leading replacement for
the hand-maintained list, and the file says plainly that the deciding
unknown is a number — what fraction of failed queries have a
reformulation-and-click pair in the same session — sitting in logs the
team already has.

The other two topics have not started. No review has ever run on this
topic, its triage queue is empty, no experiment item exists anywhere in
the unit, and no session is mid-flight.

The context was cleared at the phase boundary — this session opens cold
at the entry skill with nothing but its three arguments and what is on
disk.
