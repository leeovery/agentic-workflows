The `search-relevance` epic, three topics on the map. The
`relevance-measurement` research has run its course — NDCG@10 over
click-derived judgments is the working position — and the register
holds two threads learned, one open, one parked with its reason.

One thing is new since the last sitting: the `query-expansion`
discussion rerouted a question here, and it waits in this topic's
triage queue as its first entry — whether click-derived judgments decay
faster for expanded queries. Nothing has been said about it yet.

No deep dive has ever been dispatched and the store is empty; no
experiment exists; the other two topics have not started, and no other
session is mid-flight. The context was cleared at the phase boundary —
this session opens cold at the research entry with its three arguments
and what is on disk.
