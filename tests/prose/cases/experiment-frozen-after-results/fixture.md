The `search-relevance` epic, the `synonym-handling` research paused on
evidence. E1 is mid-measurement: the design froze at the briefing — the
question is the in-session recovery share of zero-result searches, the
registered rule is fifteen percent or better and behaviour-driven
expansion leads, less and the research goes to a curated source — the
run began, and the report already holds visible results: the first
twenty sessions are scored, twelve of them recovered.

The sample is a day of sandbox search-session activity at
`logs/search-sessions.log` — forty zero-result searches in all, twelve
of which recover with a clicked reformulation in the same session.

No other session is mid-flight. The context was cleared mid-run — this
session opens cold at the experiment entry with its four arguments and
what is on disk.
