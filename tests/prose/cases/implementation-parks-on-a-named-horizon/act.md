Execute skills/workflow-implementation-entry/SKILL.md with arguments
$0=feature, $1=pay, then continue into the processing skill it invokes
and run its task loop. The first task's gate answer does not answer the
gate — it puts something aside for later. Follow the prose wherever it
routes and land it where the user says. Stop once control returns to
the interrupted flow and its menu is back on screen — do not answer
that menu, and start no further task.
