A feature mid-discussion: card payments at checkout, shaped in
discovery, its discussion in progress. Four subtopics sit on the map —
capture confirmation (decided: webhooks over polling), card-data
handling (decided: the gateway's hosted fields), failed-payment
retries, still exploring — the conversation leans on "a small bounded
number of attempts with backoff" without pinning the bound or the
reset condition — and chargeback disputes, identified but untouched.

The topic is three background-review cycles deep. Each review came back
clean and was drained in an earlier sitting; each store row carries the
map snapshot its dispatch measured, and the latest snapshot matches the
current map exactly — nothing has moved since review-003 went out.

Hours have passed. The context was cleared — this session opens cold at
the entry skill with nothing but the two arguments and what is on disk.
