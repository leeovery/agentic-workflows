# Fixture — bridge-completes-the-feature

The `pay` feature stands fully implemented and unreviewed: discussion,
specification, and plan completed; both plan tasks implemented with
per-task `impl(pay): T…` commits in the world's git history and their
source and test files on disk; the implementation item completed. No
review directory, no report, no reviewed_tasks.

The work unit's status is `in-progress` — review is the last phase, so
the walk that approves it should carry the pipeline to `done` and the
work unit to `completed` through the bridge's terminal arm.
