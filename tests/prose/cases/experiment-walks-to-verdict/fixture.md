The `search-relevance` epic. The `synonym-handling` research is
mid-flight and paused on evidence: it narrowed the replacement question
to a curated managed list versus behaviour-driven expansion, hit the
number the choice turns on — what share of zero-result searches recover
by in-session reformulation — and spawned E1 to measure it. The
research item holds the evidence wait; the record holds the problem
statement and nothing else.

A day of sandbox search-session activity sits in the repo at
`logs/search-sessions.log` — forty zero-result searches, twelve of
which recover with a clicked reformulation in the same session.

No other session is mid-flight. The context was cleared at the pause —
this session opens cold at the experiment entry with its four
arguments and what is on disk.
