An existing application — user accounts, billing with invoices, webhook
delivery, tests for the lot — built over a run of feature commits before
the workflows were installed into it in the most recent commit. No work
has been started with the workflows yet, and no baseline verdict has ever
been recorded.
