The prose should have taken this path:

1. the start screen is not empty despite there being no work units — the
   roadmap section stands in, and the `r/roadmap` row routes into the
   roadmap skill's open mode
2. the home snapshot shows the map; the pull renders the working set
   with only the waiting items, and the selection resolves through its
   numbered table
3. the unit is shaped with the user — one epic for the two items — and
   the name they give is checked for collision before anything is
   created
4. the record crosses as the fenced backfill: the epic's session-001 is
   authored from the pulled items' slice of the roadmap record (their
   threads, the launch reasoning), not a copy of the whole product
   conversation — the kitchen display and loyalty material stays out
5. one creation then the joins: the work unit is created with the
   backfilled log, the two items are pulled into it, and the post-pull
   roadmap is rendered so the remainder is visible
6. discovery opens as a continuation — the fenced slice named, the
   reshape question asked — and never as an interrupted-session resume:
   no resume menu fires for a session this conversation itself opened

EXPECTED WORLD — the walk should have produced, from the harvested
roadmap:

- an epic work unit named `mvp`, in progress, registered in the project
  manifest, with `session-001.md` on disk whose Exploration carries the
  ordering and menu-management slice (and not the kitchen-display or
  loyalty material), Seed and Imports `(none)`, Map State
  `(empty — first session)`, and the discovery session still open (the
  active-session marker set)
- the roadmap's `ordering` and `menu-management` items joined to `mvp`
  (`pulled_to` naming it), `kitchen-display` and `loyalty` still waiting
  with their summaries and sources untouched
- no topics anywhere on the epic: no map items, no research or
  discussion directories — the slice is fenced but not yet decomposed
- the roadmap session logs untouched — the pull reads the record, never
  rewrites it
