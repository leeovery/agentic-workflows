A project holding a harvested product roadmap and nothing else: one
closed roadmap session laid Orderflow out into four waiting items —
customer ordering, menu management, and the kitchen display in an `mvp`
horizon, loyalty in `v1`. No work units exist; nothing has ever been
pulled into delivery.

The user is back, ready to commit a first slice to building.
