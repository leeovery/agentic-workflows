The prose should have taken this path:

1. the roadmap home renders and the pull row routes into the pull; the
   working set lists the three waiting items and the selection resolves
   through its numbered table
2. the unit is shaped with the user — one epic over the two `v2` items —
   and the name they gave while shaping is carried straight into the
   creation: nothing is suggested back to them, and no collision check
   runs ahead of the engine
3. the record is read before the backfill is authored. The parked items
   name the epic's discovery session log as their source, which is read
   in full
4. the postponed item has a record beyond its sources, and the read set
   comes from the address it carries — the epic and the topic it was
   there — not from the sources list. So the topic's brief, its concluded
   discussion, and the concern still queued against it are all read in
   full, including the queued concern that no `sources` entry names.
   Every one of them is a read: nothing under the earlier epic is
   written, moved, drained or absorbed, and its queue is never listed
   through the engine
5. the backfill is authored from what was read: the new epic's
   session-001 carries the two pulled items' slice — what the earlier
   epic settled about the expansion source, the question the queued
   concern leaves open, and the query-understanding ground — with Seed
   and Imports `(none)` and Map State `(empty — first session)`. The
   third item's material stays out: the fence is the pulled set, and the
   personalised-ranking ground is not in it
6. one creation then the joins: the work unit is created with the
   backfilled log, the two items are pulled into it, and the post-pull
   roadmap is rendered so the remainder is visible. The walk stops there,
   without invoking the skill the pull hands off to

Further claims:

- the prior record enters the backfill as ground the new epic will
  argue with, not as decisions it inherits: what `search-relevance`
  settled is recorded as where the thinking got to, and the open
  question the concern leaves is carried as open
- the earlier epic is untouched in every way. Its manifest still reads
  `synonym-handling` postponed with its stash intact, its discussion
  file and brief are byte-identical, and the queue file is still in its
  triage directory — the pull reads a record, it never moves one
- no topic is created anywhere: the pull fences a slice, it does not
  decompose it, so the new epic's map is empty and no research or
  discussion item exists on it
- the pull never runs the pull-forward: that verb brings an item into an
  epic that already exists, and this pull is what creates one

EXPECTED WORLD — the walk should have produced, from the three-item
roadmap:

- a new epic work unit, in progress, registered in the project manifest,
  with `session-001.md` on disk whose Exploration carries the
  synonym-handling and query-understanding slice — and not the
  personalised-ranking material — and the discovery session still open
  (the active-session marker set)
- the roadmap's `synonym-handling` and `query-understanding` items joined
  to it (`pulled_to` naming it), each keeping its own `summary`,
  `origin` and `sources`, and `synonym-handling` still recording where it
  was postponed from; `personalised-ranking` still waiting, untouched
- everything under `.workflows/search-relevance/` byte-identical to the
  fixture, its manifest included
- no topics on the new epic: no map items, no research or discussion
  directories
