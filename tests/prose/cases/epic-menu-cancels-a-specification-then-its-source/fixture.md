# Fixture — epic-menu-cancels-a-specification-then-its-source

The `search-relevance` epic past the definition of one grouping: both
discussions (`behavioural-ranking`, `synonym-handling`) concluded in
earlier sittings, and one specification, `expansion`, completed over
the pair — both source rows incorporated, a document on disk — with
its plan completed beneath it. The build order was sequenced after the
completion (`expansion` 1), so no stale flag is live; the map is fully
ordered and the analysis cache settled, so epic entry runs no machine
work before the dashboard. The third map topic,
`relevance-measurement`, is still fresh.

No implementation exists under any name, so the specification is free
to cancel. While it stands, both source topics are locked: the cancel
menu lists them keyless, each naming `expansion` as the reason.
Cancelling the specification unit takes it and its plan together and
frees the two discussions; the user then cancels `synonym-handling` on
its own — a cancel that discards nothing, since no proposed grouping
sits over it.
