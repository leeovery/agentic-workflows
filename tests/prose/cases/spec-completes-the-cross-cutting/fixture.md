# Fixture — spec-completes-the-cross-cutting

The `error-handling` cross-cutting concern stands with its discussion
completed and nothing beyond it: a project-wide error-contract
standard whose discussion recorded three decisions — one error
envelope for every service (code, message, correlation_id), retryable
versus terminal carried by the code range alone, and boundary-only
logging with correlation ids — plus one deferral (the client SDK
helper library).

Cross-cutting is terminal at specification, so the walk that
concludes this specification should carry the pipeline to done and
the work unit to completed through the bridge's cross-cutting
continuation. No research exists; the discovery session is closed.
