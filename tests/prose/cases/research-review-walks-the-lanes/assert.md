The prose should have taken this path:

1. the entry reads the research phase status, finds it in progress,
   and hands off to the processing skill without asking the user
   anything
2. the process detects the in-progress file, renders resume detection,
   and the user continues
3. initialisation is skipped: the walk passes through file strategy and
   the guidelines, addresses the knowledge base once as a contextual
   query — the store holds the epic's discovery context, and the session
   takes the results in stride — and routes into the epic research
   session
4. the user shares a finding about click-log retention; the session
   documents it in the research file and commits
5. the commit arms the dispatch check: no prior review row exists,
   meaningful content landed, so a review dispatches and the stubbed
   report lands at the dispatch path
6. at the next natural break the scan promotes the report, the
   findings are acknowledged, and the announce menu renders — a count
   per lane and what each asks, wording free, no finding content
7. the user opts in. The apply lane runs first: a one-item batch screen
   rendered through the engine's finding-batch surface. The user
   approves; the judgment-collection thread is amended in place with a
   dated note citing the RankBench note — the file's own source, not a
   "decision" — committed under its own subject marker, and the batch
   is recorded in one surface call
8. the apply lane empties; the walk raises the explore finding under
   the heading `Needs Investigation`, framed as an investigable
   thread — what per-query-class behaviour would cost to examine —
   with a deep-dive offered as the natural move, not dispatched. The
   turn ends awaiting the user

Presentation claims — the research lane declarations are the behaviour
under test:

- the announce names counts and what each lane asks; no finding content
- the batch screen renders from the engine, not a prose template
- the amendment cites the file's own source (the RankBench note) as
  what determines the fix — research's apply resolution, not
  discussion's decision-citing one
- the walked raise opens with `Needs Investigation` — research's
  declared heading — padded as a sub-step marker
- the raise's move offers investigation; no deep-dive is dispatched
  without the user's say

Further claims:

- exactly one review dispatch, no re-dispatch after the report lands
- the apply batch is recorded in one surface call carrying its id; the
  explore finding's surface call comes only at its raise
- the user's click-log finding is documented and committed separately
  from the review engagement commits
- nothing concludes: no topic complete, no final review, no knowledge
  indexing

EXPECTED WORLD — the fixture plus: the user's finding documented in the
research file and committed; the judgment-collection thread amended
citing the RankBench note, committed under a `(review-001 F2)` subject;
the review report on disk in the topic's cache; and the agent store row
`review-001` with both findings surfaced, standing `incorporated`.
