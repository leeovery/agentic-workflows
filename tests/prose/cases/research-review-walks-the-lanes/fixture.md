An epic — a search-relevance overhaul — with relevance-measurement, a
research-routed topic, mid-flight: candidate metrics explored (NDCG@10
leading, with a benchmark note recording click-derived labels
correlating 0.92 with human judgments), a judgment-collection thread
that still assumes human raters, and an open question on eval-set
sizing. No review has ever run on this topic.

The judgment-collection thread contradicts the file's own benchmark
note — a correction the file's own source determines. Per-query-class
behaviour is untouched ground. Both are for the review to find.

Hours have passed. The context was cleared — this session opens cold at
the entry skill with nothing but its arguments and what is on disk.
