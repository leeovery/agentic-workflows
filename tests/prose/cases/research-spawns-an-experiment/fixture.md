The `search-relevance` epic, three topics on the map. The
`synonym-handling` research is mid-flight: the hand-maintained list is
being replaced, and the file has worked the space down to two candidate
replacement sources — a curated managed list, or expansion derived from
what searchers do when a query fails.

One number sits under that choice and the file says so plainly: nobody
knows what share of zero-result searches recover when the searcher
reformulates within the same session. Behaviour-driven expansion feeds
on exactly those recoveries, so the share is what the choice turns on —
and it has never been measured. A day of sandbox search-session logs
exists in the repo at `logs/search-sessions.log`.

No experiment series exists for the topic, no review has ever run on
it, its triage queue is empty, the other two topics have not started,
and no other session is mid-flight. The context was cleared at the
phase boundary — this session opens cold at the research entry with its
three arguments and what is on disk.
