The search-relevance epic with synonym-handling reopened — and a
world-building trap set for migration 054: the reopened discussion
document carries a malformed triage heading ("## Triage:" with a
trailing colon) holding one real parked entry, and the migrations log
has 054 un-recorded. The next boot runs 054 live; its exact-match
parser cannot recognise the malformed section, so the code converts
nothing and hands over its verify addendum — the straggler is the
judgment pass's to recover. Both discussions' items are otherwise as
their conclusions left them; the knowledge store is configured and
ready.
