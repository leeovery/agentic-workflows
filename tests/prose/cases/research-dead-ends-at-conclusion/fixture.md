The `search-relevance` epic, four topics on the map. A second discovery
session added the fourth — `vector-search-migration`, research-routed:
whether retrieval should move off the lexical index onto embeddings —
and that research has now run its course.

The file answers the question and the answer is no, without qualification:
the managed options price at six to nine times the entire existing search
cluster at this catalogue size, self-hosting needs an embedding-refresh
pipeline the two part-time owners have no capacity or experience to run,
and the recall complaints that prompted the question turn out to be
query-side vocabulary mismatches a dense retriever does not address. The
file says so plainly — nothing under this topic's name is left for anyone
to weigh, and the sizing arithmetic is what is worth keeping.

The other three topics have not started. No review has ever run on this
topic, its triage queue is empty, and no session is mid-flight.

The context was cleared at the phase boundary — this session opens cold
at the entry skill with nothing but its three arguments and what is on
disk.
