An epic shaped in an earlier session: overhauling search relevance
across the catalogue. The work-type commit landed — work unit,
description, and a session log whose Exploration records the shaping —
but the session was interrupted before any topics were pulled: the map
is empty and the session is still marked active.

A new session opens at the very top — the user has come back to carry
it forward.
