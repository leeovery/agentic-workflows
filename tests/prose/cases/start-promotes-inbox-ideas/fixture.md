# Fixture — start-promotes-inbox-ideas

An initialised project with no work units — the store is up,
migrations have run, and nothing has ever been started. The inbox
holds two captured ideas, both dated 2026-01-01, in the shape the
capture skill writes (H1 title, flowing prose):

- `saved-search-filters` — save named filter combinations on the
  orders list, per-user, applied from a dropdown.
- `filter-default-view` — mark one saved filter as the user's default
  view on login; explicitly framed as related to the first.

The two ideas deliberately describe one coherent feature so the
working set is type-uniform and the shaping conversation has a single
honest reading: one feature, not two, and not an epic. No archived
items exist.
