The `search-relevance` epic, three topics on the map. The
`relevance-measurement` research has run its course: NDCG@10 over
click-derived judgments is the working position, and the file holds how
harnesses elsewhere treat a change that lifts the aggregate while the
rare searches get worse — most hold it back, one weighs it — with the
choice between the two left for the discussion.

The register carries four threads and they do not all end learned. The
brief's question about what a good harness looks like and the
slice-regression thread are learned — the file answers both. The user's
question about how often click-derived judgments need refreshing is
still open; nothing looked at settles it. And a human-raters panel,
considered as a check on the clicks, is parked with its reason on the
row: no budget for raters this year.

No deep dive has ever been dispatched on the topic and the store is
empty; the triage queue is empty; no experiment exists; the other two
topics have not started, and no other session is mid-flight. The
context was cleared at the phase boundary — this session opens cold at
the research entry with its three arguments and what is on disk.
