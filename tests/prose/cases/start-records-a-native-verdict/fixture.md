The project grew up on the workflows. Its repository opened with three
days of scaffolding — an initial commit, a config-driven setup script and
a fix to it — and the workflows arrived in the next commit, on the same
scaffold. Since then one feature, `pay` (accepting card payments at
checkout), was created in the first session and nothing has been started
on it. No baseline verdict has ever been recorded.
