Execute skills/workflow-implementation-entry/SKILL.md with arguments
$0=feature, $1=pay, then continue into the processing skill it invokes.
The plan holds no work left to run. Stop once whatever the skill does
about that has finished writing to the plan — begin none of the work it
adds, and run no second pass over the implementation.
