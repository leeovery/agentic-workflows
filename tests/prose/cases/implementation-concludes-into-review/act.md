Execute skills/workflow-implementation-entry/SKILL.md with arguments
$0=feature, $1=pay, then continue into the processing skill it invokes.
The plan holds no work left to run. Follow the flow to its conclusion;
when the prose reaches the pipeline continuation, continue into the
bridge skill it invokes with the stated arguments, and stop where the
continuation presents its plan for approval — the flow's terminal
handoff.
