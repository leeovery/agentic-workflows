# Fixture — epic-entry-resequences-a-stale-order

The `search-relevance` epic stands mid-definition: two concluded
discussions, a sequenced discovery map, and two specification items
born ordered — `behavioural-ranking` (order 1) and `synonym-handling`
(order 2, still proposed). The `behavioural-ranking` specification has
just completed, which set `phases.specification.build_order_stale` —
its declared dependencies are the information that sharpens an order
first assigned at grouping.

The discovery map needs no sequencing (every topic ordered at the
harvest), both analysis caches are settled (the gap analysis stamped
over the two conclusions, no completed research), and the
active-session marker is cleared. The only pending machine work on
entry is the build-order refresh.
