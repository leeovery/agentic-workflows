The prose should have taken this path:

1. initialisation runs the boot pipeline — no migrations, knowledge
   ready — and the discovery dump shows one active epic, routing to
   the active-work display; the first scripted answer selects the epic
2. continue-epic loads the shared framework, opens with its phase title,
   and reads its own discovery dump; the work unit arrived as an
   argument, so no selection menu renders and validation runs the
   scoped snapshot directly
3. the backfill checks find nothing, topic discovery reads the
   gap-analysis cache as absent and dispatches nothing, and the map is
   already sequenced, so the sequencing reference is never loaded
4. the epic dashboard renders with the three-topic map, and the second
   scripted answer picks the `d` command option — start a discussion on
   a new topic; the soft gate is fetched for that selection and comes
   back empty, and the route is invoked with epic and the work unit,
   no topic
5. discussion entry, with no topic argument, asks what topic to
   discuss; the third scripted answer names `synonym-handling`, which
   is kebab-cased as given and treated as the fresh-entry source
6. before deriving any summary or reading any phase status, the entry
   fetches the direct-entry gate for the discussion phase of that name
7. the engine answers the blocker pair — the name is already on the
   map, routed to research with nothing started — and the entry emits
   both sections verbatim and stops as a terminal condition: no
   summary derivation, no discovery-item ensure, no phase validation,
   no context gathering, no handoff

Further claims:

- the walk is a read: no discussion item, no research item, and no
  map item is created; the map's three topics and their order fields
  are untouched; nothing is committed
- the discussion processing skill is never invoked
