The walk resumes an epic's specification into its review, meets a
decision the specification made and no source did, finds nothing that
settles it, and sends the ground to the roadmap rather than to a
document. Nothing reopens, no topic is born, and the specification
runs on and concludes in the same sitting.

The prose should have taken this path:

1. the entry validates the completed source discussion and the
   in-progress specification item, and hands off to the processing
   skill
2. resume detection offers the choice and the user continues; session
   setup resets both gate modes to `gated` and finds no stale rows
   and no consult references; construction finds the source
   incorporated and nothing left to extract — no content is
   re-presented — and the epic's document-dependencies step runs
   between construction and review
3. review cycle 1 initialises — `review_cycle` set to 1 with the
   construction baseline word count in the same write, committed.
   Claims verification runs first and returns clean through its stub
   with no tracking file
4. input review runs next and returns one finding through its stub,
   having written the c1 input tracking file; the tracking entry
   records `in-progress` and commits; the findings summary renders
   from the tracking file
5. the finding's Category is Unsourced decision, so the dispose keeps
   it as a `route`: it is never presented at a batch screen and never
   at a finding gate, and nothing of it is applied to the
   specification. The route names behavioural-ranking — this
   specification's one source — as the document that should own the
   missing decision
6. classification lands on the no-sides branch: the record frames no
   alternatives to choose between, so the conflict gate is never
   rendered. The derivation is attempted first and runs out — nothing
   in either discussion, the discovery session log, or the tree pins
   a weighting, and no measurement can break the tie because
   relevance measurement is an unexplored topic with no evaluation
   set behind it
7. so the session **STOPS** conversationally and puts the question to
   the user — what the specification asserts, what was searched and
   where the record ran out, what the answer unlocks — taking a
   stance. No engine surface renders for this: it is an exchange, not
   a gate, and the gate mode is `gated`, so no auto-override
   announcement is owed either
8. the user's answer shows the question needs more than this session
   can give — a relevance objective is a capability of its own, and
   the epic has nothing to score one against — so the session takes
   the gap exit rather than landing an answer. Nothing is written
   into the behavioural-ranking discussion
9. the gap exit re-reads the spec item's status first and finds it
   live, then raises the engine-rendered gap acknowledgement in its
   gap-route variant: what is missing, what was searched, what cannot
   be written until it is decided, and — this being an epic — a menu
   that names the reopen's cost and asks `Reopen it?` over three
   homes: reopening behavioural-ranking with the gap and pausing
   here, opening a new topic on the map, and parking it on the
   roadmap
10. the user takes the roadmap row. The session asks which horizon the
    capability belongs to — an existing label or a new one — and
    **STOPS**; there are no horizons yet, so whatever the user names
    is new. They name `v2`
11. the park runs as one engine call: a kebab-case capability name
    derived from the gap, the horizon the user named, a one-line
    summary of the gap, origin `park:search-relevance`, and the
    specification's own path as the source. The verb creates the
    roadmap node and the horizon just-in-time and commits the project
    manifest itself. The session tells the user in one line what was
    parked and where
12. the pause section takes the nothing-was-routed arm — every gap
    this pass raised was parked, so the specification is not paused,
    no pause commit is made, no navigation skill is invoked, and the
    session is handed straight back to the findings walk. A walk that
    paused here, told the user the specification was blocked, or
    bounced to the epic menu has taken the wrong arm
13. back in the routes, the finding lands by what the exit actually
    did: nothing landed in a document and nothing reopened, so
    nothing is owed to behavioural-ranking — but the content the
    finding indicted is a decision no source made, and the capability
    is the roadmap's now, so the 30/70 weighting comes out of the
    specification, which states no rule for it. The tracking row
    records **Declined** with the roadmap item named in its Notes,
    announced in a line, and commits
14. no route remains, so the cycle-1 input tracking entry flips to
    `complete` and commits; gap analysis runs third and returns clean
    through its stub, writing no tracking file
15. findings were surfaced and the mode is `gated`, so the
    convergence analysis is reached and returns without a diagnostic
    — one cycle of tracking data is below its two-cycle threshold —
    and the re-loop gate renders in its reloop variant. The walk
    **STOPS**
16. the user proceeds to completion. The compliance self-check runs,
    then the epic's cross-cutting assessment is put to the user and
    confirmed as directly plannable; tracking verifies complete and
    the one source verifies incorporated; the sign-off gate renders
    and the walk **STOPS** a final time
17. on the user's yes the topic completes through the engine, the date
    is stamped, and the conclusion commits with the knowledge index
    riding it. No source specification exists to supersede
18. the walk stops at the pipeline continuation without invoking the
    bridge

The end world's claims:

- the project manifest carries a roadmap node it did not have before:
  one horizon, the one the user named, holding exactly one item — a
  kebab-case capability name for the ranking-objective question, with
  `origin: park:search-relevance`, a summary that reads as the
  capability rather than as a review finding, and the specification's
  own path among its sources. No other roadmap item exists, and the
  item carries no `pulled_to`: it is waiting, joined to nothing
- the discovery map carries the same three topics it started with.
  Nothing named for the weighting appears on it, and no research or
  discussion item was created — the roadmap is not the map
- the behavioural-ranking discussion item still reads `completed` and
  its document file is byte-identical to the fixture's — no timeline
  entry, no new subtopic, no edits of any kind. The synonym-handling
  discussion is untouched in both item and document
- the behavioural-ranking specification item reads `completed` with a
  date, `sources.behavioural-ranking.status` still `incorporated`,
  and no second source row: the park added nothing to `sources`,
  which is what lets this specification conclude at all
- the specification no longer states the 30/70 weighting anywhere:
  the park settles that the ground is not this specification's to
  fill, so the rule nobody decided is removed rather than corrected,
  and the surrounding Signal Ingestion and Ranking Features content
  stands. A specification still carrying the weighting has parked the
  capability and kept the invented rule. A Dependencies section from
  the epic's own step is the one addition the pass is allowed to have
  made
- the c1 input tracking file is on disk with its one finding resolved
  **Declined**, its Notes naming the roadmap item; no row reads
  Pending, and the manifest's tracking entry for it reads `complete`
- `finding_gate_mode` and `construction_gate_mode` both read `gated`;
  neither was ever set to auto
- the git history carries the engine's own roadmap commit over the
  project manifest, and no commit whose message says the
  specification paused. Nothing is left dirty
- the stops the user met were the resume choice, the unmade decision
  (in conversation, not at a gate), the gap gate, the horizon ask,
  the re-loop prompt, the cross-cutting assessment and sign-off,
  plus the dependencies section the epic's step presents for
  approval. They were never offered sides to pick between, never
  shown a batch screen, and never asked to approve a finding
