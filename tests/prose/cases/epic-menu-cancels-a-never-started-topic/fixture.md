# Fixture — epic-menu-cancels-a-never-started-topic

The `search-relevance` epic just after its harvest: three topics on the
map with briefs, summaries, and descriptions, the map sequenced
(`behavioural-ranking` 1, `synonym-handling` 2, `relevance-measurement`
3), the discovery session closed. No research, discussion, or
specification exists under any name — every topic is fresh, and
`relevance-measurement` is a topic the user has decided not to do.

No analysis cache is owed (nothing concluded), every order is present,
and no specification exists, so epic entry runs no machine work before
the dashboard. The cancel menu lists the three topics under one group,
all pickable; nothing is cancelled, so the epic menu carries no
reactivate option until the walk lands one.
