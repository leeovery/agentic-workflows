A feature mid-discussion: card payments at checkout, two subtopics
decided on the map, one open, the discussion file carrying both decided
sections.

The previous sitting made two calls the record settles — webhook
signature verification, refund currency — and queued them to the calls
queue rather than asking, as the session prescribes. It died before the
next natural break: the queue file sits in the topic's cache with both
calls in `items` and nothing in `pulled`, nothing yet documented,
nothing committed. No background agent has ever been dispatched.

Hours have passed. The context was cleared — this session opens cold at
the entry skill with nothing but the two arguments and what is on disk.
