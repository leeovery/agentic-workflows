A feature shaped in discovery: accept card payments at checkout using
the existing gateway account. The work unit exists with its description
and its discovery session log; no phase beyond discovery has run, and
the work has no seed and no imports.

The context was cleared at the phase boundary — this session opens cold
at the entry skill with nothing but the two arguments and what is on
disk.
