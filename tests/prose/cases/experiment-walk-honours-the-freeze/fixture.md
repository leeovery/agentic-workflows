The `pay` feature, freshly created and routed straight to experiments.
The shaping surfaced one doubt talking cannot settle: the gateway
sandbox is suspected of delivering some capture webhooks twice, and
whether idempotent webhook handling is built for v1 or deferred hangs on
how often that actually happens. The carrier says so, and a day's
sandbox export sits in the repo at `logs/webhooks.log` — sixty
deliveries, three of them duplicates.

No experiment item exists, no discussion has started, the knowledge
store is keyword-only and empty of this unit, and no other session is
mid-flight. The session opens cold at the experiment entry with its
arguments and what is on disk.
