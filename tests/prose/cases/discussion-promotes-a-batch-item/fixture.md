A feature mid-discussion: card payments at checkout, two subtopics
decided — capture confirmation (webhooks, never polling) and currency
handling (integer minor units).

A background review returned three findings and put all three in the
apply lane. Two of them are: a Decision sentence the Journey retired,
and a rule stated for amounts but left implied for refunds. The third
is not settled at all — the Journey records that reconciling a webhook
that never arrives was *raised and not settled*, and the Summary's Open
Threads says so; the reviewer read the absence of polling as deciding
the reconciliation path, which nobody decided.

The store row is acknowledged and announced. Nothing has been surfaced.

Hours have passed. The context was cleared — this session opens cold at
the entry skill with nothing but the two arguments and what is on disk.
