A feature mid-discussion: card payments at checkout, three subtopics
decided — capture confirmation (webhooks, never polling), currency
handling (integer minor units), and failed-payment retries, which
defers to the gateway's own ceiling rather than naming a number.

A background review returned three findings and put all three in the
apply lane. Two are sound: a Decision sentence the Journey retired, and
a rule stated for amounts but left implied for refunds.

The third is a misfile nothing in the document reveals. It reads the
retry Decision, sees it defers to a ceiling it never names, and
proposes the provider's documented figure of three. From the artifact
alone that is a clean correction — the Decision already defers to that
source, so the reviewer is only writing down what it points at. What
the document does not record, and cannot, is that this account sits on
a negotiated plan whose ceiling is not the published one.

The store row is acknowledged and announced. Nothing has been surfaced.

Hours have passed. The context was cleared — this session opens cold at
the entry skill with nothing but the two arguments and what is on disk.
