The prose should have taken this path:

1. the plan gate renders empty; the implementation item exists
   in-progress, so the entry proceeds as a resume — the reconcile
   advisory finds no flag and stays silent; dependency validation
   returns immediately
2. the entry hands off into the processing skill
3. resume detection reports the resumed mode: the one-line resuming
   notice is displayed and no start commit is made
4. environment setup finds the existing document stating no setup is
   required and returns without asking anything
5. the plan adapter is loaded for the manifest's format
6. project skills discovery reads the populated topic value and
   returns immediately — no gate, no render surface, no output; the
   project default is never consulted
7. linter setup does the same on its populated topic value
8. the walk stops as the skill turns to its next concern — the
   knowledge guidance is never loaded and the task loop is never
   entered

Further claims:

- neither setup surface was fetched in any variant, and the user was
  asked nothing between the handoff and the stopping point
- nothing was written: no manifest field moved, no cache payload was
  authored, and the world is unchanged from the fixture
