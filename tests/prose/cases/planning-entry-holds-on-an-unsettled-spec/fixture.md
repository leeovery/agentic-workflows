A feature whose plan is authored and graphed — two approved phases,
three tasks in local markdown with dependencies and priorities applied,
the spec baseline recorded — and whose specification moved beneath it
between sittings. A peer session reopened the card-payments discussion,
which staled the specification's extracted source row and flagged the
specification itself.

Nobody has re-entered the specification, so its own status still reads
`completed`. The plan still reads in-progress.

The context was cleared mid-phase — this session opens cold at the
entry skill with nothing but the two arguments and what is on disk.
