The prose should have taken this path:

1. initialisation runs the boot pipeline and the start screen renders
   the active work — the `launch` epic — with the `r/roadmap` row; the
   first answer routes into the roadmap skill's open mode
2. the roadmap home renders from its snapshot, `mvp` showing ordering
   in delivery beside its two waiting items; the second answer resolves
   to the converse action through the home's ACTIONS entry
3. a fresh product session opens — no session was left open — and
   the user places gift cards in `mvp` themselves: a direct add, no
   more shaping owed
4. the add is aimed at a horizon with a member in delivery, so the
   routed confirm renders before anything is added: the three-way menu
   (menu management and the kitchen display still wait), its DATA
   naming `launch` among the work units the delivery row pulls into;
   the prose stops for the answer
5. the user's reply is a question, not a pick: the gate is set aside
   and the question answered in product terms — the delivery row makes
   gift cards part of the `launch` epic's scope now, beside ordering;
   the waiting row leaves them on the roadmap until a later pull —
   then the prose asks whether they are ready, and nothing is added or
   joined while the talk runs
6. on the user's yes the gate is rendered again — a second fetch of
   the same surface, never the first emission repeated from memory —
   and the prose stops for the answer again
7. the user picks waiting: a plain `roadmap add` of the item into
   `mvp`, the horizon quoted, sourced to the product session's log —
   no pull-forward, no work unit touched — and the add is recorded
   under the log's Edits, the log conjured on this first state change
8. the loop turns back to the conversation and the walk stops there

Further claims:

- no answer at the gate was inferred from the question: the add waits
  for the pick that follows the re-render
- the session is not laid out, harvested, or closed

EXPECTED WORLD — the walk should have produced, from the fixture:

- a new roadmap item for gift cards — a capability-grain kebab name,
  a one-line summary in product terms — in the `mvp` horizon,
  waiting: no `pulled_to` join
- `ordering` still joined to `launch`; `menu-management`,
  `kitchen-display` and `loyalty` unchanged
- a second roadmap session log (`session-002.md`) under
  `.workflows/.roadmap/sessions/`, still open (the active-session
  marker naming it), whose Edits record the gift-cards add into `mvp`;
  session 001 untouched
- the `launch` epic untouched: no discovery map items, no topics, no
  research or discussion files
