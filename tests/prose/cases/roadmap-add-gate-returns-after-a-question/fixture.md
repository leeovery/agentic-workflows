A project holding the harvested Orderflow roadmap with its first slice
in delivery: customer ordering was pulled from the `mvp` horizon into
the `launch` epic, whose discovery has opened and gone no further.
Menu management and the kitchen display still wait in `mvp` beside
it; loyalty waits in `v1`. No roadmap session is open.

The user comes back to the roadmap with a new launch capability in
mind — gift cards — and wants to understand what placing it beside
work already underway would mean before they choose.
