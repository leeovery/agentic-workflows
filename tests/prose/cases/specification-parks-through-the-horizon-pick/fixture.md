A feature whose discussion is complete: card payments at checkout,
decided through — the existing gateway account, card-only for v1 with
wallets deferred, and capture confirmed by gateway webhooks with no
polling.

The specification was opened in an earlier sitting and got no further
than that: the file exists carrying the body template alone — a title,
an empty Specification section, an empty Working Notes section — the
item is in progress, and its one source still reads `pending`. Nothing
has been extracted and nothing has been approved.

The product already has a roadmap: two horizons in release order, `v1`
holding `wallet-payments` and `v2` holding `subscription-billing`, both
waiting and joined to no work unit.

The context was cleared between sittings — this session opens cold at
the specification entry skill with nothing but the two arguments and
what is on disk.
