The prose should have taken this path:

1. workflow-start boots (migrations + knowledge gate through engine
   boot), shows the active work, and routes into the epic's continue
   skill for search-relevance; backfill, topic discovery, and both
   sequencing steps are silent (the gap-analysis cache is stamped over
   the settled pair, every map order is present, no specification is on
   the board)
2. the epic dashboard renders — two decided topics, one fresh — and the
   menu carries `p/postpone` beside `a/cancel`
3. choosing `p` fetches the postpone-menu sub-view: one `Topics` group,
   three numbered rows in map order, none locked, no `Specifications`
   group — postpone addresses the Discovery unit and no other
4. picking Synonym Handling stores the topic and hands straight to the
   shared postpone door; the menu composes no gate of its own
5. the door reads the work type first and finds an epic, so the unit is
   the Discovery stage's under the topic's own name. The menu is no
   session in a topic, so nothing is committed and no agent store is
   scanned anywhere in this walk
6. the user named no horizon, so the roadmap state is read. It holds no
   horizons — there is no roadmap — so the horizon is asked for in
   prose, in the user's own staging words. The numbered horizon pick is
   never rendered: that arm belongs to a map that exists
7. the confirm is fetched from the engine at
   `search-relevance.discovery.synonym-handling` with the named horizon,
   and its menu emitted, and the flow STOPs. The statement is the
   engine's — the flow never composes what the postpone takes or where
   it lands — and nothing is written before the yes
8. on yes one postpone transaction runs, carrying the horizon. Nothing
   else writes: no `roadmap add`, no discovery-map call, no manifest
   write by hand, no cancel
9. the receipt is fetched from the engine and emitted, naming the
   horizon the topic now waits under. The response carries no discarded
   grouping and no reverted join — the item was born, not re-waited — so
   the flow adds no line of its own
10. the dashboard re-renders with Synonym Handling gone from the tree and
    named on the compact postponed line with its horizon; the walk stops
    there with nothing selected

Further claims:

- cancel is never reached for: no `cancel-gate` renders, no cancel
  transaction runs, and the map row carries no `cancelled` marker.
  Postpone and cancel are different endings and only one of them ran
- the roadmap skill is never invoked and no park gate renders — a park
  puts an idea on the map, and this is a topic leaving an epic

EXPECTED WORLD — from the fixture:

- the manifest's discussion item for `synonym-handling` reads
  `postponed` and carries `previous_status: completed`; the map row
  carries `postponed: true` with its `order` stashed as
  `previous_order`. The other two map rows are untouched, and their
  orders still read 1 and 3
- the discussion file is still on disk with its Expansion Source section
  intact, and the topic's brief is where the harvest wrote it — a
  postpone takes the item, never the document
- the project manifest now holds a roadmap: one horizon, `v2`, holding
  one item named for the topic, its summary the map row's, its origin
  the postpone of this epic, recording the work unit and topic it came
  from and pointing at the topic's brief and discussion files
- no research, specification, planning, implementation, or review item
  exists under any name; no second work unit; the work-unit status is
  still in progress
- git history holds the engine's own postpone commit over both
  manifests, and nothing else was committed
