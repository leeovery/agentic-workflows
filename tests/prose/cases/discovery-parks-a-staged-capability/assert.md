The prose should have taken this path:

1. the epic is continued from the start menu and its interrupted first
   discovery session resumed — the prior exploration briefed, the
   conversation picked up rather than restarted
2. when the merchandiser-analytics capability surfaces and the user
   stages it beyond this epic ("phase two"), the session treats it as a
   park, not a topic: placement is proposed from the user's own words
   and confirmed, never assumed
3. the park is capture-weight — one engine call lands it (the roadmap
   is born at that first park; no ceremony, no session at product
   level), the session log records it under Edits, and the conversation
   carries on without derailing
4. the exploration's running record is written at a natural pause and
   committed

Further claims:

- the capability never appears as a discovery-map topic, and the epic's
  scope is unchanged — the park is how the epic said no cheaply
- the park's provenance names this epic as its origin and points its
  source at this session's log

EXPECTED WORLD — the walk should have produced, from the epic alone:

- a `roadmap` node newly on the project manifest: one horizon named
  from the user's staging words (phase-two-shaped), holding one waiting
  item for the merchandiser analytics capability with
  `origin: park:search-relevance` and a source pointing at the epic's
  `discovery/sessions/session-001.md`
- the epic's session log grown, not restructured: Exploration extended
  with the resumed conversation's ground, an Edits entry recording the
  park, Topics Identified and Conclusion still `(none)`, the
  active-session marker still set
- no discovery-map items, no research or discussion directories, no new
  work units, and no roadmap sessions — the map exists, item-first
