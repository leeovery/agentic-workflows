The prose should have taken this path:

1. the entry resolves the topic from its arguments, reads the research
   status, finds it in progress, emits the resuming phase note, checks
   the reconcile flag (absent — silent), and hands off to the processing
   skill without asking the user anything; the epic context arm reads the
   map item's source, finds it map-shaped rather than direct-start, and
   gathers nothing
2. the process reads the status again, finds the file, renders the
   thread register once above resume detection — three threads, one
   being dug, one open, one learned, the header's breakdown naming all
   three — and the user continues; initialisation is skipped
3. the walk passes through file strategy and the guidelines, addresses
   the knowledge base once as a contextual query, and routes into the
   epic research session — the deep-dive and rerouted-concerns
   protocols loaded, nothing run at load
4. the session loop's first iteration checks what landed: the triage
   queue reads empty, and the resumed session's first iteration is a
   natural break, so the walk reads the store — the scan answers one
   pending row, `deep-dive-001-tail-regressions`, and nothing in flight.
   No row is closed as dead: the dive landed, and the landed arm is the
   one taken
5. the fold runs as one transaction of judgment, in the prose's order:
   the report is read in full from the store's content path; a section
   headed by the thread's question, the dive's id, and the date is
   written into the research file — the two answers first, then the
   material, the sources kept inline; the thread is set `learned` and
   the row closed clean; the Opened list is judged line by line — the
   labelling question becomes a thread of origin `deep-dive-001` nested
   under the folded thread, the query-log count is held as the
   laboratory's cue — and one commit lands with the dive's id in its
   subject, the section, the thread's move, and the opened thread
   together and nothing unrelated
6. the session speaks the return to the user — the brief asked
   questions, so both answers reach them in full — and renders the
   register: the folded thread now learned with its opened child beneath
   it, the breakdown moved accordingly
7. the Opened list named a measurement, so the laboratory is offered
   next, conversationally and in the session's own voice — the count a
   controlled measurement would settle, what a dependable answer takes
   off the table, that an inline count is a fine alternative — never a
   script, never a menu; the turn ends there awaiting the user, and the
   walk stops

Altitude claims — the return is the behaviour under test:

- the answers open on the product, not the report: what happens to a
  ranking change that lifts most searches and drops the rare ones is on
  the page before any tool, file, function, or job is named — most of
  the harnesses surveyed hold the change back when a group of searches
  gets worse past a small allowance, one lets it through when the gain
  elsewhere outweighs a loss it counts more heavily; and a group of
  searches is either declared and rebuilt on a schedule with a minimum
  size before it counts, or worked out fresh each time from how often
  searches run
- the substance of both answers is present in full — three hold and one
  weighs, where the hold sits, the allowance before it fails, the size
  floor, the rebuild, the derived-at-evaluation alternative — nothing
  the brief asked is left for the user to go and read
- no code identifier from the report appears in the turn — not the
  guard function, the exception, the CI step, the slices file, the
  rebuild job, the weights table, the notebook call, nor a snippet, a
  path, or a cron line; a mechanism is told only as what the product
  does, and a name reaches the user only where they would have to go and
  look at it, which nothing here requires
- the report is never pasted: no fenced block, section heading, or
  paragraph of it appears verbatim in the conversation; the file
  carries the report's material, the conversation carries its meaning
- nothing is put to the user as a question in the fold: the material
  opens a product choice — hold on a slice, or weigh it — and the
  choice is left for the discussion, named as material rather than
  asked; no announce menu, no lanes, no accept-or-dismiss of anything

Further claims:

- the dive's id and the row's id are used where each belongs: every
  `agent` verb takes the row id with its label; the register origin and
  the commit subject carry the bare `deep-dive-001`
- the opened thread nests under `tail-regressions` — the folded thread
  is top-level, so it is the parent
- the measurement line becomes no thread: it is the laboratory's cue,
  and the offer is made rather than the count recorded — no experiment
  is created, no problem statement written, no now-or-later gate
  rendered, and nothing is measured against any log
- no fresh dispatch of any kind, no dive offer for the opened question
  (it is carried as a thread, not sent out), no review, no conclusion
- the register's other rows are untouched: the brief's thread stays
  learned, the user's label-freshness thread stays open
- git history holds the fold commit alone on top of the fixture, its
  subject carrying `(deep-dive-001)`

EXPECTED WORLD — the fixture plus: the research file carrying a new
section headed by the folded question, `deep-dive-001`, and the date,
answers first then material, sources inline; the register with
`tail-regressions` learned, a child thread under it of origin
`deep-dive-001` and status `open` carrying the labelling question,
`evaluation-harness` learned and `label-freshness` open exactly as the
fixture left them; the store row `deep-dive-001-tail-regressions`
`incorporated` with no findings; the report file still on disk; no
experiment item, no change to the map or the sibling topics.
