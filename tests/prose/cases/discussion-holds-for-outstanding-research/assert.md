The prose should have taken this path:

1. the entry resolves the topic from its arguments, reads the
   discussion status, finds it in progress, and emits the resuming
   phase note; the reconcile check finds `research` and, before
   anything else, reads the research item's status — `triaged` — so
   the advisory's outstanding branch runs: the callout says research on
   this topic is still outstanding (parked), that decisions here may
   rest on ground it re-examines, that this work cannot conclude until
   it lands, and that the menu's research row is the way in. The flag
   is left in place — no delete — and no research file is read, there
   being none
2. the epic context arm reads the map item's source, finds it
   map-shaped, gathers nothing, and hands off with source: existing
   discussion — the user is asked nothing at the entry
3. the process renders resume detection — the map with both subtopics
   decided — and the user continues; initialisation is skipped, the
   guidelines load, the knowledge base is addressed once as a
   contextual query, and the session loop's first triage check reads
   the discussion queue and finds it empty: the parked concern sits in
   the research queue, which this session never reads or drains
4. the user asks to wrap up. The concluding ceremony has no calls to
   flush and fetches the wait gate before anything is deferred; the
   gate comes back populated — the blocker naming research on
   Behavioural Ranking as parked and not yet started, the guidance
   naming the research row as the way in and a cancel as the release,
   and the pause/keep menu — emitted verbatim, and the session stops.
   The map gate never runs and nothing is written `deferred`; the
   closing gates are never loaded and no completion is attempted — the
   session never asks the engine to do what it would refuse
5. the user takes the pause; the cadence commit runs (nothing to
   commit is a valid answer), the session says where the ball sits —
   the ceremony runs once what the discussion waits on has landed, the
   menu carries the way in — and stops at the terminal condition

Further claims:

- the reconcile flag survives the whole walk:
  `discussion.behavioural-ranking` still carries
  `reconcile_needed: research` — the advisory holds it while the
  research is outstanding, and nothing else clears it
- the research stub is untouched: `research.behavioural-ranking` still
  reads `triaged`, its queue still holds `001-signal-density.md` with
  its content intact, and no research file exists — the session never
  started, drained, or cancelled the research
- the discussion item stays `in-progress`; both map subtopics stay
  `decided`, and no subtopic was added or deferred
- the discussion document's decisions stand unchanged — neither was
  reopened or rewritten on a measurement nobody has taken; a Summary
  touch-up is the most the session may have written
- no experiment was spawned, no review walk blocked the close, and the
  knowledge base was never written
- the user was asked two things: the resume choice, and the pause/keep
  choice at the blocked conclusion

EXPECTED WORLD — the fixture, or nearly: the manifest unchanged (flag
held, stub parked, discussion in progress, both subtopics decided);
the research queue file intact; at most the discussion document's
Summary touched and committed by the pause's cadence commit; no
knowledge-base change.
