The prose should have taken this path:

1. the entry validates the in-progress research and routes to resume;
   the process finds the research file and puts the resume choice to
   the user, who continues
2. the resumed session re-reads its ground — both threads hold
   conclusions — and the user wraps up rather than opening anything
   new
3. the background review dispatches through the engine and returns
   clean through the harness stub; the final gap review finds nothing
   to add
4. document review re-reads the document, then sweeps the
   unverified-claims dimension: the Instrumentation Surface count is
   load-bearing enough to check, its recorded command is re-run, and
   the measurement contradicts it — five modules, not four
5. because the thread's conclusion is per-module and survives the
   corrected value, the fix is applied in place: the count and its
   command read the measured truth, the conclusion is untouched, no
   raise is put to the user, and the change is committed and mentioned
   in the review brief
6. conclusion puts the wrap-up to the user, and on their yes the topic
   completes through the engine — the artifact indexes as part of that
   call — and the conclusion commits
7. the walk stops at the hand-off to the pipeline bridge without
   invoking it

Further claims:

- the research item moves in-progress → completed exactly once; no
  reopen, no triage landing
- nothing outside .workflows changes: the checkout modules are read
  and counted, never edited
- cache and scratch files under `.workflows/.cache/` are expected
  working artifacts

EXPECTED WORLD — from a feature holding an in-progress research file
and five checkout modules:

- the research's Instrumentation Surface thread reads the measured
  truth — five modules, the command with its corrected result — with
  its conclusion untouched and nothing narrating the session that
  fixed it
- the rest of the document as the fixture wrote it, give or take
  wrap-up polish that adds no new conclusions
- the manifest holding the research item completed, and no phase
  beyond research
- the five files under src/checkout/ byte-identical to the fixture
- no discussion, specification, or later artifacts anywhere
