The prose should have taken this path:

1. the empty project is shown as such, and starting unsure routes into
   discovery with no pre-seed
2. the shaping conversation reads as product-altitude — a brand-new
   product with staged ambition, no single unit of work on the table —
   and the gate states that read; on the user's confirm the flow invokes
   the roadmap's genesis mode instead of committing a work type
3. the roadmap session opens with the shaping conversation backfilled
   into its Exploration — persisted before the conversation continues,
   because pre-commit shaping was ephemeral
4. the conversation continues at product altitude — staging language
   listened for, placements surfaced softly, nothing parked (at this
   altitude it is all in scope) — until the user asks to sort
5. the harvest proposes capability-grain items in horizons named from
   the user's own language, renders the proposal over the (empty)
   roadmap, and persists the confirmed set in one batch
6. document review reconciles the log, the session closes (marker
   cleared, log indexed), and the stop-or-pull gate is answered with
   stop — a complete outcome, nothing owed

Further claims:

- no work unit is ever created, named, or offered for creation before
  the stop — the whole walk commits the user to building nothing
- the horizons come from the conversation (launch-now wording and
  later staging), not from a fixed vocabulary imposed on it

EXPECTED WORLD — the walk should have produced, from a project that held
no work at all:

- a `roadmap` node on the project manifest: ordered horizons in the
  user's own staging words (a launch/MVP-shaped first horizon, a later
  one, and a someday-shaped tail for the white-label ambition), and
  items covering the capabilities discussed — customer ordering, menu
  upkeep, kitchen visibility in the first horizon; loyalty later;
  white-label reselling in the tail
- every item waiting (no `pulled_to` anywhere), each with a one-line
  product-first summary and a `sources` pointer at the session log
- exactly one roadmap session log, `session-001.md` under
  `.workflows/.roadmap/sessions/`, its Exploration carrying the product
  intent and the staging reasoning, its Items Sorted matching the
  confirmed set, its Conclusion finalised — and no active-session marker
  left on the manifest
- no work units: no `.workflows/{unit}` directories, no registrations
